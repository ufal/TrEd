<?xml version="1.0" encoding="utf-8"?>
<article xmlns:pod="http://axkit.org/ns/2000/pod2xml">

<title/>

<section>
<title>TredMacro</title>
<para>
TredMacro (<filename>tred.def</filename>) - implements the public API for TrEd/bTred macros
</para>
<section>
<title>Global variables</title>
<variablelist><varlistentry><term><literal>$this</literal></term><listitem>
<para>
Current node (i.e. the active node in TrEd and the node in turn if
<literal>-N</literal> or <literal>-H</literal> flag was used in bTrEd). Assigning a different node
from the current tree to this variable results in changing the active
node in TrEd and continuing processing at that node in bTrEd.
</para>
</listitem></varlistentry><varlistentry><term><literal>$root</literal></term><listitem>
<para>
Root of the current tree. If possible, avoid changing this variable in
a macro, so that called macros may rely on its value too.
</para>
</listitem></varlistentry><varlistentry><term><literal>$FileChanged</literal> (alias <literal>$FileNotSaved</literal> - still valid but obsolete)</term><listitem>
<para>
See also <literal>ChangingFile()</literal>.
</para>
<para>
If this variable is set to 1, TrEd/bTrEd considers the current file to
be modified and in case of bTrEd makes the program to save it before
it is closed (in case of TrEd, user is prompted before the file is
closed). If the macro does not change this variable, bTrEd does not
change the file status, while TrEd still assumes that the file <emphasis>was</emphasis>
modified. In other words, set this variable to 1 in bTrEd if you want
the file to be saved at last, and set this variable to 0 in TrEd if
you are sure you did not make any change to the file that would be
worth saving it. As there is a danger that calling one macro from
another may result in a mess in the value of <literal>$FileChanged</literal> it is
adviced to use the default macro <literal>ChangingFile()</literal> which tries to set
the file status only if really intended (see below).
</para>
</listitem></varlistentry><varlistentry><term><literal>$Redraw</literal></term><listitem>
<para>
This variable makes sense only in TrEd.  You may set it to one of
<literal>file</literal>, <literal>tree</literal>, <literal>win</literal>, <literal>all</literal>, <literal>tie</literal> to alter the mechanism of redrawing the
screen. The default value is <literal>file</literal> (redraw all windows displaying
current file), while <literal>tree</literal> means redraw all windows displaying
current tree, <literal>win</literal> means redraw only current window and <literal>tie</literal> means
redraw all if windows are tied or (if not) redraw only current window.
To disable redrawing set this variable to <literal>none</literal>.
</para>
</listitem></varlistentry><varlistentry><term><literal>$forceFileSaved</literal></term><listitem>
<para>
In TrEd, you may wish to set this variable to 0 if you wish to change
the status of the file to <literal>saved</literal> (e.g. after saving the file from
your macro).
</para>
</listitem></varlistentry><varlistentry><term><literal>$libDir</literal></term><listitem>
<para>
This variable contains a path to TrEd library directory.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Navigation</title>
<para>
Methods of <link linkend="">Treex::PML::Node</link> 
objects should be used for basic navigation within
trees. Here are described means to navigate from one tree to another
and a few extra macros for specific navigation in trees.
</para>
<variablelist><varlistentry><term><literal>GotoTree(n)</literal></term><listitem>
<para>
Display the n'th tree in the current file.
The number of the first tree for this function is 1.
</para>
</listitem></varlistentry><varlistentry><term><literal>TieGotoTree(n)</literal></term><listitem>
<para>
Go to n'th tree in all tied windows.
The number of the first tree for this function is 1.
</para>
</listitem></varlistentry><varlistentry><term><literal>TieNextTree()</literal></term><listitem>
<para>
Display the next tree in all tied windows.
</para>
</listitem></varlistentry><varlistentry><term><literal>TiePrevTree()</literal></term><listitem>
<para>
Display the previous tree in all tied windows.
</para>
</listitem></varlistentry><varlistentry><term><literal>NextTree()</literal></term><listitem>
<para>
Display the next tree in the current file.
</para>
</listitem></varlistentry><varlistentry><term><literal>PrevTree()</literal></term><listitem>
<para>
Display the previous tree in the current file.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetTrees()</literal></term><listitem>
<para>
Return a list of trees in current <link linkend="">Treex::PML::Document</link>. Equivallent to
<literal>CurrentFile()-&gt;trees</literal>.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetSecondaryFiles($fsfile?)</literal></term><listitem>
<para>
Return a list of secondary <link linkend="">Treex::PML::Document</link> objects for the given (or current)
file.  A secondary file is a file required by a file to be loaded
along with it; this is typical for files containing some form of a
stand-off annotation where one tree is built upon another. Note
however, that this does not include so called knitting - an operation
where the stand-off annotation is handled by a IO backend and the
resulting knitted file appears to btred as a single unit.
Only those secondary files that are already open are returned.
</para>
</listitem></varlistentry><varlistentry><term><literal>NextNode(node,top?)</literal></term><listitem>
<para>
Return the first displayed node following the given node in the
subtree of top. This function behaves in the same manner as the
node-&gt;following(top) method, except it works only on the nodes which
are actually visible according to the state of the View-&gt;Show Hidden
Nodes menu item.
</para>
</listitem></varlistentry><varlistentry><term><literal>PrevNode(node,top?)</literal></term><listitem>
<para>
Return the first displayed node preceding the given node in the
subtree of top. This function behaves in the same manner as the
node-&gt;previous(top) method, except it works only on the nodes which
are actually visible according to the state of the View-&gt;Show Hidden
Nodes menu item.
</para>
</listitem></varlistentry><varlistentry><term><literal>NextVisibleNode(node,top?)</literal></term><listitem>
<para>
Return the first visible node following the given node in the subtree
of top. This function behaves in the same manner as the
<literal>$node-&gt;following($top)</literal> method, except that nodes of hidden subtrees
are skipped.
</para>
</listitem></varlistentry><varlistentry><term><literal>PrevVisibleNode(node,top?)</literal></term><listitem>
<para>
Return the first visible node preceding the given node in the subtree
of top. This function behaves in the same manner as the
<literal>$node-&gt;previous($top)</literal> method, except that nodes of hidden subtrees
are skipped.
</para>
</listitem></varlistentry><varlistentry><term><literal>IsHidden(node)</literal></term><listitem>
<para>
Return true if the given node is member of a hidden subtree. This
macro is only an abbreviation for
<literal>FS()-&gt;isHidden(node)</literal>
</para>
</listitem></varlistentry><varlistentry><term><literal>Hide(node)</literal></term><listitem>
<para>
Hide a given node.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetNodes(top?)</literal></term><listitem>
<para>
Get a list of all nodes in the current tree or (if top is given) in
the subtree of top (the root of the tree is icluded as well). The list
returned is ordered in the depth-first ordering.
(This function automatically returns array reference in scalar context.)
</para>
</listitem></varlistentry><varlistentry><term><literal>GetVisibleNodes(top?)</literal></term><listitem>
<para>
Return the list of all visible nodes in the subtree of the given top
node (or the whole current tree if no top is given). The list returned
is ordered in the depth-first ordering and all members of hidden
subtrees are skipped.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetDisplayedNodes($win?)</literal></term><listitem>
<para>
Return the list of all nodes actually currently displayed in the
current window in TrEd (which nodes are actually displayed can be
e.g. specified with get_nodelist_hook).
</para>
</listitem></varlistentry><varlistentry><term><literal>PrevNodeLinear(node,attribute,top?)</literal></term><listitem>
<para>
Returns nearest node in the tree preceding the given node in linear
ordering provided by the given attribute. If top node is present, only
a subtree of top is examined.
</para>
</listitem></varlistentry><varlistentry><term><literal>NextNodeLinear(node,attribute,top?)</literal></term><listitem>
<para>
Returns nearest node in the tree following the given node in linear
ordering provided by the given attribute. If top node is present, only
a subtree of top is examined.
</para>
</listitem></varlistentry><varlistentry><term><literal>CurrentTreeNumber( $win? )</literal></term><listitem>
<para>
Return current tree number.  Note that the number of the first tree if
file reported by this function is 0, so you have to add 1 before
passing the number to functions like GotoTree.  If used in TrEd,
optional argument win can be used to specify TrEd window (defaults to
the current window).
</para>
</listitem></varlistentry><varlistentry><term><literal>GetNodeIndex()</literal></term><listitem>
<para>
Return given node's position in the deep-first tree ordering.
</para>
</listitem></varlistentry><varlistentry><term><literal>LocateNode(node?,fsfile?)</literal></term><listitem>
<para>
Return current filename, index of a tree (starting from 1) in the file
to which the node belongs (0 if not found) and node's position in the tree
in the deep-first tree ordering.
</para>
<para>
If <literal>node</literal> is not given, <literal>$this</literal> is assumed. Should the node be from a
different file than the current one, the second argument must specify
the corresponding <link linkend="">Treex::PML::Document</link> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>ThisAddress(node?,fsfile?)</literal></term><listitem>
<para>
Return a given node's address string in a form of
filename#tree_no.index (tree_no starts from 1 to reflect TrEd's UI
convention).  If the correct tree number could not be determined (the
node does not belong to any top-level tree in the file) and the node
has an ID, the address is returned in the form filename#ID.
</para>
<para>
If <literal>node</literal> is not given, <literal>$this</literal> is assumed. Should the node be from a
different file than the current one, the second argument must specify
the corresponding <link linkend="">Treex::PML::Document</link> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>ThisAddressNTRED(node?,fsfile?)</literal></term><listitem>
<para>
Return a given node's address string in a form of
ntred://filename@tree_no#1.index (tree_no starts from 1 to reflect
TrEd's UI convention). If the correct tree number could not be
determined (the node does not belong to any top-level tree in the
file) and the node has an ID, the address is returned in the form
ntred://filename#ID.
</para>
<para>
The returned address may be opened in TrEd to examine the tree in
memory of a remote btred server.
</para>
<para>
If <literal>node</literal> is not given, <literal>$this</literal> is assumed. Should the node be from a
different file than the current one, the second argument must specify
the corresponding <link linkend="">Treex::PML::Document</link> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>FPosition(node?,fsfile?)</literal></term><listitem>
<para>
Prints the result of <literal>ThisAddress</literal> on stdout.
</para>
</listitem></varlistentry><varlistentry><term><literal>NPosition(node?,fsfile?)</literal></term><listitem>
<para>
Prints the result of <literal>ThisAddressNTRED</literal> on stdout.
</para>
</listitem></varlistentry><varlistentry><term><literal>ParseNodeAddress(address)</literal></term><listitem>
<para>
Split given address into a filename/URL and a suffix and return these
as a two element list.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Tree editing API</title>
<variablelist><varlistentry><term><literal>CutPaste(node,new-parent)</literal></term><listitem>
<para>
Cut given node (including its subtree) and paste it to a new
parent. This macro is safer than PasteNode since it checks that
new-parent is not a descendant of node or node itself. If the check
fails, the macro dies with an error before any change is made.
</para>
<para>
Note: this macro does not verify node types. Use
<literal>$parent-&gt;test_child_type($node)</literal> to be sure that type declarations permit
the node as a child node of the new parent.
</para>
</listitem></varlistentry><varlistentry><term><literal>CutPasteBefore(node,ref_node)</literal></term><listitem>
<para>
Cut given node (including its subtree) nd paste it on ref_node's
parent node just before ref_node. This macro is safer than
PasteNodeBefore since it checks that new-parent is not a descendant of
node or node itself. This macro dies on error before any change is
made.
</para>
<para>
Note: this macro does not verify node types. Use
<literal>$parent-&gt;test_child_type($node)</literal> to be sure that type declarations permit
the node as a child node of the new parent.
</para>
</listitem></varlistentry><varlistentry><term><literal>CutPasteAfter(node,ref_node)</literal></term><listitem>
<para>
Cut given node (including its subtree) and paste it on ref_node's parent node just after
ref_node. This macro is safer than PasteNodeBefore since it checks
that new-parent is not a descendant of node or node itself. This macro
dies on error before any change is made.
</para>
<para>
Note: this macro does not verify node types. Use
<literal>$parent-&gt;test_child_type($node)</literal> to be sure that type declarations permit
the node as a child node of the new parent.
</para>
</listitem></varlistentry><varlistentry><term><literal>PasteNode(node,new-parent)</literal></term><listitem>
<para>
Paste the subtree of the node under the new-parent.  The root of the
subtree is placed among other children of new-parent with respect to
the numbering attribute.
</para>
<para>
Note: this macro does not verify node types. Use
<literal>$parent-&gt;test_child_type($node)</literal> to be sure that type declarations permit
the node as a child node of the new parent.
</para>
</listitem></varlistentry><varlistentry><term><literal>PasteNodeBefore(node,ref_node)</literal></term><listitem>
<para>
Cut given node (including its subtree) and paste it on ref_node's
parent node just before ref_node.
</para>
<para>
Note: this macro does not verify node types. Use
<literal>$parent-&gt;test_child_type($node)</literal> to be sure that type declarations permit
the node as a child node of the new parent.
</para>
</listitem></varlistentry><varlistentry><term><literal>PasteNodeAfter(node,ref_node)</literal></term><listitem>
<para>
Cut given node (including its subtree) and paste it on ref_node's
parent node just after ref_node.
</para>
<para>
Note: this macro does not verify node types. Use
<literal>$parent-&gt;test_child_type($node)</literal> to be sure that type declarations permit
the node as a child node of the new parent.
</para>
</listitem></varlistentry><varlistentry><term><literal>CloneSubtree(node)</literal></term><listitem>
<para>
Return an identical copy (except that only declared attributes are
preserved) of the given subtree.
</para>
</listitem></varlistentry><varlistentry><term><literal>CopyNode(node)</literal></term><listitem>
<para>
Return an identical copy (except that only declared attributes are
preserved) of the given node. The input node must belong to the
current file.
</para>
</listitem></varlistentry><varlistentry><term><literal>CutNode(node)</literal></term><listitem>
<para>
Cut the node's subtree off the tree and return it. By cuttin a subtree
we mean disconnecting it from the rest of the tree. Use PasteNode to
attach it to some node again.
</para>
</listitem></varlistentry><varlistentry><term><literal>NewTree()</literal></term><listitem>
<para>
Create a new tree before the current tree. The new tree consists of
exactly one node. This node is activated and a reference to its <link linkend="">Treex::PML::Node</link>
object is returned.
</para>
</listitem></varlistentry><varlistentry><term><literal>NewTreeAfter()</literal></term><listitem>
<para>
Create a new tree after the current tree. The new tree consists of
exactly one node. This node is activated and a reference to its <link linkend="">Treex::PML::Node</link>
object is returned.
</para>
</listitem></varlistentry><varlistentry><term><literal>NewRBrother(node)</literal></term><listitem>
<para>
Create a new brother of the given node and recalculate the special FS
numbering attribute values in the whole tree so that the new node is
the first right sibling of the given node.
</para>
<para>
If no node is given, this function operates on <literal>$this</literal> and <emphasis>resets</emphasis>
<literal>$this</literal> to the newly created node. If some node is given the value
of <literal>$this</literal> is preserved.
</para>
</listitem></varlistentry><varlistentry><term><literal>NewLBrother(node)</literal></term><listitem>
<para>
Create a new brother of the given node and recalculate the special FS
numbering attribute values in the whole tree so that the new node is
the first left sibling of the given node.
</para>
<para>
If no node is given, this function operates on <literal>$this</literal> and <emphasis>resets</emphasis>
<literal>$this</literal> to the newly created node. If some node is given the value
of <literal>$this</literal> is preserved.
</para>
</listitem></varlistentry><varlistentry><term><literal>NewSon(parent)</literal></term><listitem>
<para>
Create a new child of the given parent node and recalculate the
special FS numbering attribute values in the whole tree so that the
new node is the first node right to the given parent.
</para>
<para>
If no parent node is given, this function operates on <literal>$this</literal> and
<emphasis>resets</emphasis> <literal>$this</literal> to the newly created node. If a parent node is
given the value of <literal>$this</literal> is preserved.
</para>
</listitem></varlistentry><varlistentry><term><literal>NewParent(node)</literal></term><listitem>
<para>
Create a node between given node and its parent and recalculate the
special FS numbering attribute values in the whole tree so that the
new node is the first node left to the given node.
</para>
<para>
If no node is given, this function operates on <literal>$this</literal> and
<emphasis>resets</emphasis> <literal>$this</literal> to the newly created node. If a parent node is
given the value of <literal>$this</literal> is preserved.
</para>
</listitem></varlistentry><varlistentry><term><literal>DeleteThisNode()</literal></term><listitem>
<para>
Delete the current (<literal>$this</literal>) node and recalculate the special FS
numbering attribute values in the whole tree so that there is no gap
in the numbering. If the current node is not a leaf or if it is the
root of the current tree, this macro does nothing.
</para>
</listitem></varlistentry><varlistentry><term><literal>DeleteLeafNode(node)</literal></term><listitem>
<para>
Delete a leaf node and recalculate the special FS numbering attribute
values in the whole tree so that there is no gap in the numbering. If
a given node is not a leaf, this macro does nothing.
</para>
</listitem></varlistentry><varlistentry><term><literal>DeleteSubtree(node)</literal></term><listitem>
<para>
Deletes a whole node's subtree and recalculate the special FS
numbering attribute values in the whole tree so that there is no gap
in the numbering.
</para>
</listitem></varlistentry><varlistentry><term><literal>RemoveTree(n?,fsfile?)</literal></term><listitem>
<para>
Removes n-th tree from a given <link linkend="">Treex::PML::Document</link> (trees are numbered starting from 0).
If no <link linkend="">Treex::PML::Document</link> is specified, uses the current file (see CurrentFile()).
</para>
<para>
Calling RemoveTree() without arguments, it is equivalent to
</para>
<literallayout>
RemoveTree(CurrentTreeNumber(),CurrentFile())
</literallayout>
<para>
Returns the deleted tree. If you do not reattatch the tree to the <link linkend="">Treex::PML::Document</link>,
use $tree-&gt;destroy method on the returned tree to prevent a memory leak.
</para>
</listitem></varlistentry><varlistentry><term><literal>DestroyTree(n?,fsfile?)</literal></term><listitem>
<para>
Like RemoveTree but immediatelly destroys the removed tree. Returns 1
on success.
</para>
</listitem></varlistentry><varlistentry><term><literal>DetermineNodeType($node)</literal></term><listitem>
<para>
For trees with <link linkend="">Treex::PML::Schema</link>-based node typing, try to determine the type
of the node from the type of its parent, and associate the node with
the type (using $node-&gt;set_type). If there are more possibilities, the
user is asked to choose the correct type.
</para>
</listitem></varlistentry><varlistentry><term><literal>CopyValues()</literal></term><listitem>
<para>
Copy the values of all the attributes except the special FS numbering
attribute of the current node to a global hash variable named
%ValuesClipboard.
</para>
</listitem></varlistentry><varlistentry><term><literal>PasteValues()</literal></term><listitem>
<para>
Replace existing values of the current node's attributes by all values
stored in the global hash variable named %ValuesClipboard. The
function does not perform any node-type validity checks.
</para>
</listitem></varlistentry><varlistentry><term><literal>PlainNewSon(parent)</literal></term><listitem>
<para>
Add a new child node to the given parent and make it the current
node (by setting <literal>$this</literal> to point to it).
</para>
</listitem></varlistentry><varlistentry><term><literal>PlainDeleteNode()</literal></term><listitem>
<para>
Delete the given node. The node must be a leaf of the tree (may not
have any children) and must have a parent (may not be the root of the
tree).
</para>
</listitem></varlistentry><varlistentry><term><literal>PlainDeleteSubtree(node)</literal></term><listitem>
<para>
Cut a the given node's subtree and destroy all its nodes.
This macro does not recalculate ordering attributes.
</para>
</listitem></varlistentry><varlistentry><term><literal>NormalizeOrds(listref)</literal></term><listitem>
<para>
Adjusts the special FS numbering attribute of every node of the list
referenced by the listref argument so that the value for the attribute
corresponds to the order of the node in the list and repaste each node
so that the structural order corresponds with this new numbering.
</para>
<para>
If no numbering FS numbering attribute is available, all this macro
does is to ensure that the structural order of sibling nodes
corresponds to their order in the given list.
</para>
</listitem></varlistentry><varlistentry><term><literal>SortByOrd(listref)</literal></term><listitem>
<para>
Sort the list of nodes referenced by the listref argumnt according to
the values of the special FS numbering attribute.
</para>
</listitem></varlistentry><varlistentry><term><literal>RepasteNode(node)</literal></term><listitem>
<para>
Cut the given node and paste it immediately on the same parent so that
its structural position between its parent children is brought to
correspondence with the values of the special FS numbering attribute.
</para>
</listitem></varlistentry><varlistentry><term><literal>ShiftNodeRightSkipHidden(node)</literal></term><listitem>
<para>
Shift the current node in the tree to the right leaping over all
hidden subtress by modifying the tree structure and value of the
special FS numbering attribute appropriately.
</para>
</listitem></varlistentry><varlistentry><term><literal>ShiftNodeLeftSkipHidden(node,min?)</literal></term><listitem>
<para>
Shift the current node in the tree to the left leaping over all hidden
subtress by modifying the tree structure and value of the special FS
numbering attribute appropriately. The optional argument min may be
used to specify the minimum left boundary for the value of the
ordering attribute of node.
</para>
</listitem></varlistentry><varlistentry><term><literal>ShiftNodeRight(node)</literal></term><listitem>
<para>
Shift the current node in the tree to the right by modifying the tree
structure and value of the special FS numbering attribute
accordingly.
</para>
</listitem></varlistentry><varlistentry><term><literal>ShiftNodeLeft(node)</literal></term><listitem>
<para>
Shift the current node in the tree to the right by modifying the tree
structure and value of the special FS numbering attribute
appropriately.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetNodesExceptSubtree(node)</literal></term><listitem>
<para>
Returns the reference to an array containing the whole tree except the
nodes strictly in the subtrees of nodes given in an array referenced
by the parameter. (The returned array does contain the nodes passed in
the parameter.)
</para>
</listitem></varlistentry><varlistentry><term><literal>MoveNode(source-node,target-node)</literal></term><listitem>
<para>
Move the node specified by the first parameter after the node specified by
the second parameter in the ordering on nodes.
</para>
</listitem></varlistentry><varlistentry><term><literal>MoveSubtree(source-node,target-node)</literal></term><listitem>
<para>
Move the subtree of the node specified by the first parameter after
the node specified by the second parameter in the ordering on
nodes. The subtree of the first argument is made contiguous in the
ordering on nodes, if it happens not to be so. (We use the fact that
the technical root node is always first in the ordering.)
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Helper macros for attributes with list or alternatives of values</title>
<variablelist><varlistentry><term><literal>CloneValue(value)</literal></term><listitem>
<para>
Return an identical deep copy of a given scalar. Useful for copying
attribute values (and any value below them), including structured
attributes, lists or alternatives.
</para>
<para>
Warning: do not apply this macro to an entire <link linkend="">Treex::PML::Node</link> since otherwise
you might result with a copy of the complete tree, schema, etc.
</para>
</listitem></varlistentry><varlistentry><term><literal>IsList(value)</literal></term><listitem>
<para>
Check that a given value is a list, i.e. <link linkend="">Treex::PML::List</link> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>IsAlt(value)</literal></term><listitem>
<para>
Check that a given value is an alternative, i.e. <link linkend="">Treex::PML::Alt</link> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>IsSeq(value)</literal></term><listitem>
<para>
Check that a given value is a sequence, i.e. <link linkend="">Treex::PML::Seq</link> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>List(value,value,...)</literal></term><listitem>
<para>
Return a new list (<link linkend="">Treex::PML::List</link> object) populated with given values.
</para>
</listitem></varlistentry><varlistentry><term><literal>Alt(value,value,...)</literal></term><listitem>
<para>
Return a new alternative (<link linkend="">Treex::PML::Alt</link> object) populated with given values.
</para>
</listitem></varlistentry><varlistentry><term><literal>AltV(value)</literal></term><listitem>
<para>
If the value is an alternative (i.e. a <link linkend="">Treex::PML::Alt</link> object), return
all its values. Otherwise return value.
</para>
</listitem></varlistentry><varlistentry><term><literal>ListV(value)</literal></term><listitem>
<para>
If the value is a list (i.e. a <link linkend="">Treex::PML::List</link> object), return
all its values. Otherwise return empty (Perl) list.
</para>
</listitem></varlistentry><varlistentry><term><literal>SeqV(value)</literal></term><listitem>
<para>
If the value is a sequence (i.e. a <link linkend="">Treex::PML::Seq</link> object), return all its
elements (<link linkend="">Treex::PML::Seq::Element</link> objects). Otherwise return empty (Perl)
list.
</para>
</listitem></varlistentry><varlistentry><term><literal>AddToAlt(node,attr,value,value...)</literal></term><listitem>
<para>
Add given values as alternatives to the current value of
<literal>$node-&gt;{$attr}</literal>.  If only one value is given and <literal>$node-&gt;{$attr}</literal> is
empty or same as value, the given value is simply assigned to it. If
<literal>$node-&gt;{$attr}</literal> is a <link linkend="">Treex::PML::Alt</link> object, the new values are simply added
to it. Otherwise, if <literal>$node-&gt;{$attr}</literal> is a simple value, <literal>$node-&gt;{$attr}</literal>
is set to a new <link linkend="">Treex::PML::Alt</link> object containing the original value as
well as the given values.
</para>
</listitem></varlistentry><varlistentry><term><literal>AddToList(node,attr,value,value,...)</literal></term><listitem>
<para>
Add values to a given attribute. If <literal>$node-&gt;attr($attr)</literal> is not defined or
empty, a new <link linkend="">Treex::PML::List</link> containing given values is created. If
<literal>$node-&gt;attr($attr)</literal> is a <link linkend="">Treex::PML::List</link> object, given values are simply added
to it. Error is issued if <literal>$node-&gt;attr($attr)</literal> is defined, non-empty, yet not
a <link linkend="">Treex::PML::List</link> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>AddToListUniq(node,attr,value,value,...)</literal></term><listitem>
<para>
Add values to a given attribute, unless already present among the
current values. If <literal>$node-&gt;attr($attr)</literal> is not defined or empty,
a new <link linkend="">Treex::PML::List</link> containing given values is created. If
<literal>$node-&gt;attr($attr)</literal> is a <link linkend="">Treex::PML::List</link> object, given values are
simply added to it. Error is issued if <literal>$node-&gt;attr($attr)</literal> is
defined, non-empty, yet not a <link linkend="">Treex::PML::List</link> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>AddToSeq(node, attr, name =</literal> value, name =&gt; value,...)&gt; or <literal>AddToSeq(node, attr, element, ...)</literal></term><listitem>
<para>
Add elements to a given sequence attribute. If <literal>$node-&gt;attr($attr)</literal> is not defined or
empty, a new <link linkend="">Treex::PML::Seq</link> object containing given name-value pairs (<link linkend="">Treex::PML::Seq::Element</link> objects) is created. If
<literal>$node-&gt;attr($attr)</literal> is a <link linkend="">Treex::PML::Seq</link> object, given elements are simply added
to it. Error is issued if <literal>$node-&gt;attr($attr)</literal> is defined, non-empty, yet not
a <link linkend="">Treex::PML::Seq</link> object.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>General-purpose list functions</title>
<variablelist><varlistentry><term><literal>Index(array-ref,item)</literal></term><listitem>
<para>
A helper function which returns the index of the first occurence of a
given item in an array. Returns nothing (empty list) if the item is
not found. Note: 'eq' is used for comparison.
</para>
</listitem></varlistentry><varlistentry><term><literal>ListIntersect(array-ref,array-ref,...)</literal></term><listitem>
<para>
Compute intersection of given lists. In scalar context returns an
array-ref, in list context returns a list. All duplicities are
removed.
</para>
</listitem></varlistentry><varlistentry><term><literal>ListSubtract(array-ref, array-ref)</literal></term><listitem>
<para>
Return elements occuring in the first list but not in the second list.
In scalar context returns a <link linkend="">Treex::PML::List</link> object (array-ref), in list
context returns a list. All duplicities are removed.
</para>
</listitem></varlistentry><varlistentry><term><literal>ListUnion(array-ref, array-ref, ...)</literal></term><listitem>
<para>
Return union of given lists. In scalar context returns an array-ref,
in list context returns a <link linkend="">Treex::PML::List</link> object (array-ref). All
duplicities are removed.
</para>
</listitem></varlistentry><varlistentry><term><literal>ListRegroupElements(array-ref, array-ref, ...)</literal></term><listitem>
<para>
This is rotate-matrix like operation. The input is a list of
rows (array-refs each representing a row in a matrix); the output
is a list of columns in the matrix (a list of array-refs, each
representing a column in the matrix).
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>GUI-related macros</title>
<variablelist><varlistentry><term><literal>GUI()</literal></term><listitem>
<para>
Return a reference if running from TrEd, i.e., GUI is available;
otherwise return <literal>undef</literal>. If running from TrEd the returned value is
a TrEd::Window object representing the currently focused window.
</para>
</listitem></varlistentry><varlistentry><term><literal>CloseGUI()</literal></term><listitem>
<para>
This macro is only available in TrEd. It closes TrEd's main window and
exits.
</para>
</listitem></varlistentry><varlistentry><term><literal>TrEdWindows()</literal></term><listitem>
<para>
Return a list of TrEd::Window objects representing current TrEd
windows.
</para>
</listitem></varlistentry><varlistentry><term><literal>CurrentWindow()</literal></term><listitem>
<para>
Return a TrEd::Window object representing the currently focused
window.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetCurrentWindow(win)</literal></term><listitem>
<para>
Focus a given window.
</para>
</listitem></varlistentry><varlistentry><term><literal>SplitWindowHorizontally(\%opts?)</literal></term><listitem>
<para>
Split current window horizontally. Returns an object represening the
newly created window.
</para>
<para>
An optional HASHref with the following options can be passed as an argument:
</para>
<variablelist><varlistentry><term>no_focus =&gt; 0|1</term><listitem>
<para>
do not focus the new view (default value is 1)
</para>
</listitem></varlistentry><varlistentry><term>no_init =&gt; 0|1</term><listitem>
<para>
do not initialize the new view with the file, context, etc. from the
current vie (default value is 0)
</para>
</listitem></varlistentry><varlistentry><term>no_redraw =&gt; 0|1</term><listitem>
<para>
do not redraw the new view
</para>
</listitem></varlistentry><varlistentry><term>ratio =&gt; float</term><listitem>
<para>
A float value between 0 and 1 indicating the ratio of the height of
the new view to the current height of the active view.
</para>
</listitem></varlistentry></variablelist></listitem></varlistentry><varlistentry><term><literal>SplitWindowVertically()</literal></term><listitem>
<para>
Split current window horizontally. Returns an object represening the
newly created window.
</para>
<para>
An optional HASHref with the following options can be passed as an argument:
</para>
<variablelist><varlistentry><term>no_focus =&gt; 0|1</term><listitem>
<para>
do not focus the new view (default value is 1)
</para>
</listitem></varlistentry><varlistentry><term>no_init =&gt; 0|1</term><listitem>
<para>
do not initialize the new view with the file, context, etc. from the
current vie (default value is 0)
</para>
</listitem></varlistentry><varlistentry><term>no_redraw =&gt; 0|1</term><listitem>
<para>
do not redraw the new view
</para>
</listitem></varlistentry><varlistentry><term>ratio =&gt; float</term><listitem>
<para>
A float value between 0 and 1 indicating the ratio of the height of
the new view to the current height of the active view.
</para>
</listitem></varlistentry></variablelist></listitem></varlistentry><varlistentry><term><literal>CloseWindow(win?)</literal></term><listitem>
<para>
Close a given window, unless it is the last window that exists.  If no
window is given as an argument, try closing the current window.  If
the closed window was focused, then a next sibling window gets focus.
</para>
</listitem></varlistentry><varlistentry><term><literal>CurrentFile(win?)</literal></term><listitem>
<para>
Return a <link linkend="">Treex::PML::Document</link> object representing the file currently open in a given
TrEd window or in the currently focused window if called without
arguments.
</para>
</listitem></varlistentry><varlistentry><term><literal>CurrentNodeInOtherWindow(win)</literal></term><listitem>
<para>
Return the node currently active in a given window.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetCurrentNodeInOtherWindow(win,node)</literal></term><listitem>
<para>
Set active node for a given window.
</para>
</listitem></varlistentry><varlistentry><term><literal>InVerticalMode()</literal></term><listitem>
<para>
Return true if the tree is currently displayed in the vertical mode.
</para>
</listitem></varlistentry><varlistentry><term><literal>ToplevelFrame()</literal></term><listitem>
<para>
Returns the Tk::Toplevel object containing the current window.
</para>
</listitem></varlistentry><varlistentry><term><literal>PrintDialog(...)</literal></term><listitem>
<para>
See the description in the <link linkend="sect-0">Printing trees</link> section.
</para>
</listitem></varlistentry><varlistentry><term><literal>Redraw($win?.$ignoreThis)</literal></term><listitem>
<para>
Force TrEd to immediately redraw the current (or given) window. Hence
TrEd redraws the tree right after an interactively invoked macro
finishes, explicit calls to Redraw macro are needed rather rearly (for
example from a hook). If the flag $ignoreThis is used, then current
value of $this variable will not be used to determine the current node
of the window (i.e. $win-&gt;{currentNode}).
</para>
</listitem></varlistentry><varlistentry><term><literal>Redraw_FSFile()</literal></term><listitem>
<para>
Force TrEd to immediately redraw all windows displaying current
file.
</para>
</listitem></varlistentry><varlistentry><term><literal>Redraw_FSFile_Tree()</literal></term><listitem>
<para>
Force TrEd to immediately redraw all windows displaying current
tree.
</para>
</listitem></varlistentry><varlistentry><term><literal>Redraw_All()</literal></term><listitem>
<para>
Force TrEd to immediately redraw all windows.
</para>
</listitem></varlistentry><varlistentry><term><literal>RedrawStatusLine()</literal></term><listitem>
<para>
Force TrEd to immediately redraw status line.
</para>
</listitem></varlistentry><varlistentry><term><literal>EditAttribute(node,attribute)</literal></term><listitem>
<para>
Open edit attribute GUI.
</para>
</listitem></varlistentry><varlistentry><term><literal>Undo()</literal></term><listitem>
<para>
Act as if the user pressed the undo button. Note that this macro also
changes $this and $root (since undo replaces the current tree with an
in-memory copy).
</para>
<para>
WARNING: Be aware that calling Undo() from a hook may conflict with
some macros, more specifically, with macros triggering such a hook
(indirectly, i.e. via some API call). In a case, changes to $this and
$root caused by Undo() do not propagate to the macro, which in turn
ends up working with incorrect (already replaced) nodes. Thus, if
calling Undo() within a hook, make sure to reset $this from
$grp-&gt;{currentNode} and $root from $this-&gt;root after all calls from
your macros which may possibly invoke the hook.
</para>
</listitem></varlistentry><varlistentry><term><literal>Redo()</literal></term><listitem>
<para>
Act as if the user pressed the undo button. Note that this macro also
changes $this and $root.
</para>
</listitem></varlistentry><varlistentry><term><literal>SaveUndo(comment)</literal></term><listitem>
<para>
Save a snapshot of the current tree so that following operations on it
can be reverted with Undo. The <literal>comment</literal> argument should provide
user-visible one-line diescription of the action following SaveUndo.
</para>
</listitem></varlistentry><varlistentry><term><literal>ForgetRedo()</literal></term><listitem>
<para>
Disable redo. This is useful if you programmatically Undo user's
action and do not want the user to be able to redo the action with
Redo.
</para>
</listitem></varlistentry><varlistentry><term><literal>Find()</literal></term><listitem>
<para>
Open the Find Node by Attributes GUI dialog.
</para>
</listitem></varlistentry><varlistentry><term><literal>FindNext()</literal></term><listitem>
<para>
Searches for the first node matching the criteria of the previous use
of the Find... menu command or FindNode macro usage.
</para>
</listitem></varlistentry><varlistentry><term><literal>FindPrev()</literal></term><listitem>
<para>
Searches for the previous node matching the criteria of the previous
use of the Find... menu command or FindNode macro usage.
</para>
</listitem></varlistentry><varlistentry><term><literal>ErrorMessage(message)</literal></term><listitem>
<para>
In TrEd, show a dialog box containing the given error-message in a
text window.  In BTrEd print the error message on standard error output.
</para>
</listitem></varlistentry><varlistentry><term><literal>InfoMessage(message)</literal></term><listitem>
<para>
In TrEd, show a dialog box containing the given info-message in a
text window.  In BTrEd print the message on standard output.
</para>
</listitem></varlistentry><varlistentry><term><literal>StandardTredFont()</literal></term><listitem>
<para>
Return a string or Tk::Font object representation of the font used in
TrEd to label tree-nodes.
</para>
</listitem></varlistentry><varlistentry><term><literal>StandardTredValueLineFont()</literal></term><listitem>
<para>
Return a string or Tk::Font object representation of the font used in
TrEd to display the "sentence" above the tree.
</para>
</listitem></varlistentry><varlistentry><term><literal>CenterOtherWinTo(win,node)</literal></term><listitem>
<para>
Center given window to a given node.
</para>
</listitem></varlistentry><varlistentry><term><literal>HiddenVisible(win?)</literal></term><listitem>
<para>
Return true if TrEd displays hidden nodes in a given window (or the
currently focused window if called without an argument).
</para>
</listitem></varlistentry><varlistentry><term><literal>ToggleHiding($win?)</literal></term><listitem>
<para>
If TrEd displays hidden nodes (or the currently focused window if
called without an argument), hide them and vice versa.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Create/modify keyboard shortcuts</title>
<variablelist><varlistentry><term><literal>Bind( macro =&gt; bind-options,...)</literal></term><listitem>
<para>
This macro can be used to dynamically create keyboard bindings and
menu items for user-defined macros in TrEd. The arguments consist of
one or more (macro =&gt; bind-options ) pairs
where macro is a subroutine name or a CODE reference and
bind-options is either a keyboard shortcut (e.g. 'Ctrl+a') or a HASH references
with the following keys and values (all optional):
</para>
<variablelist><varlistentry><term>context</term><listitem>
<para>
the binding context, e.g. the package in which context the macro is
evaluated. Only relevant if the macro is specified by name. If not
given, the caller package is used.
</para>
</listitem></varlistentry><varlistentry><term>key</term><listitem>
<para>
a keyboard binding for the macro (e.g. 'Ctrl+a'); if not given, no
keyboard binding is created.
</para>
</listitem></varlistentry><varlistentry><term>menu</term><listitem>
<para>
a label to be used for a menu-item created for the macro. If not
given, no menu-item is created for the macro.
</para>
</listitem></varlistentry><varlistentry><term>changing_file</term><listitem>
<para>
if the key <literal>changing_file</literal> exists in the bind-options hash and its
value is <emphasis>value</emphasis>, a call to the macro <literal>ChangignFile(<emphasis>value</emphasis>)</literal>
is made immediatelly after each invocation of the macro via this
binding.
</para>
</listitem></varlistentry></variablelist></listitem></varlistentry><varlistentry><term><literal>UnbindBuiltin(key-binding)</literal></term><listitem>
<para>
(TrEd only). Remove default TrEd's keybinding for a given key so that
custom macro can be bound to that key.
E.g. UnbindBuiltin('Ctrl+Home'); Note: use with care. Built-in
key-bining once removed cannot be restored (except by restarting TrEd).
</para>
</listitem></varlistentry><varlistentry><term><literal>OverrideBuiltinBinding(context,key-binding, binding_spec )</literal></term><listitem>
<para>
(TrEd only). Get or modify default TrEd's keybinding.
</para>
<para>
The <literal>binding_spec</literal> argument, if given, must be an array reference with two elements:
</para>
<literallayout>
[ sub { ... }, 'description' ]
</literallayout>
<para>
WARNING: You have to be very careful when overriding a builtin
binding, since your code is not called as a macro or a hook! The routine is
called with the following three arguments:
</para>
<literallayout>
$tk_window, $grp-&gt;{framegroup}, $key
</literallayout>
<para>
You may obtain $grp from the second argument using
$_[1]-&gt;{currentWindow}.  You should probably call Tk-&gt;break at the end
of your code, so that the event does not propagate. There is no
<literal>$root</literal> or <literal>$this</literal> available within the callback.
</para>
<para>
A safer way (with slower execution, though) is to use a macro wrapped
into a MacroCallback() instead of a sub:
</para>
<literallayout>
[ MacroCallback(...), 'description' ]
</literallayout>
<para>
See <literal>MacroCallback</literal> macro for details. The macro is called with the
same arguments (appended to a list of arguments provided in
<literal>MacroCallback</literal>). The macro can use all standard macro functions and
variables.
</para>
<para>
If the <literal>context</literal> argument is a name of a binding context, the binding
is only used in the specific context.  If <literal>context</literal> is <literal>'*'</literal>, the
binding applies to all contexts (except for contexts that specifically
overrid it).
</para>
<para>
If called without a binding_spec, the current binding_spec is returned, otherwise
the previous binding_spec is returned.
</para>
<para>
Example (swaps the behavior of normal <literal>left/right</literal> with <literal>Shift+left/right</literal>)
</para>
<literallayout>
my @keys = qw(Left Right);
my %normal = map { $_ =&gt; OverrideBuiltinBinding('*',$_) } @keys;
my %shift = map { $_ =&gt; OverrideBuiltinBinding('*',"Shift+$_") } @keys;
for (@keys) {
  OverrideBuiltinBinding(__PACKAGE__, $_, $shift{$_});
  OverrideBuiltinBinding(__PACKAGE__, "Shift+$_", $normal{$_});
}
</literallayout>
<para>
Here is another example that changes the Up arrow to move to the
grand-parent instead of a parent:
</para>
<literallayout>
# Using TrEd internals (faster, but prone to changes in the internal API):

OverrideBuiltinBinding(
  __PACKAGE__,
  'Up',
  [sub { main::currentUp($_[1]) for 1..2 },
  'Move to grand-parent']);

# Using a macro (slower):

OverrideBuiltinBinding(
  __PACKAGE__,
  "Up",
  [MacroCallback(sub { $this=$this-&gt;parent; $this=$this-&gt;parent if $this }),
  'Move to grand-parent']);
</literallayout>



</listitem></varlistentry><varlistentry><term><literal>MacroCallback(macro_spec,@args)</literal></term><listitem>
<para>
This macro can be used to safely wrap a given macro into a code that
can be used to override a TrEd builtin or passed as a callback to a Tk
widget.
</para>
<para>
The first argument can take one of the following forms:
</para>
<literallayout>
String:
  "macro_name" or "Package-&gt;macro_name"

Anonymous subrutine:
  sub { ...code... }

Hash reference:
  { -command =&gt; sub { ...code... },
    -changing_file =&gt; 0 | 1,
    -contex =&gt; 'PackageName'
  }

Array reference:
  [ sub { ..code ...}, arg1, arg2, ...]
</literallayout>



<para>
The macro is called with the arguments provided in the macro_spec,
followed by the arguments provided in the call to MacroCallback,
followed by arguments passed by the caller (e.g. TrEd or the Tk
widget).
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Stylesheet API</title>
<variablelist><varlistentry><term><literal>STYLESHEET_FROM_FILE()</literal></term><listitem>
<para>
This function returns a symbolic name for a virtual stylesheet that is
constructed from the patterns and hint specified in the currently
displayed file.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetStylesheetPatterns(patterns,stylesheet,create)</literal></term><listitem>
<para>
Set TrEd's display patterns for a given stylesheet. If stylesheet is
undefined, then the stylesheet currently selected for the active view
is used. The patterns argument should either be a string or an array
reference.  If it is a string, then it should provide all the
patterns, each pattern starting on a new line (but possibly spanning
across several lines) which starts with a pattern prefix of the form
"something:", where "something" is hint for the hint pattern, or
"node" for the node pattern, etc.
</para>
<para>
Patterns can also be provided as an array reference containing three
elements: the first one being a hint text, the second the context
pattern (regular expression), and the second one an array reference
whose each element is the text of an individual pattern.
</para>
<para>
The create flag can be set to 1 in order to create a new stylesheet in
case that no stylesheet with the given exists.
</para>
<para>
This function returns 1 if success, 0 if failed (i.e. when create is
not set and a given stylesheet is not found).
</para>
</listitem></varlistentry><varlistentry><term><literal>DeleteStylesheet(stylesheet)</literal></term><listitem>
<para>
Delete given stylesheet. All windows using that stylesheet are
switched to the pattern and hint specified in the respective files
they display.
</para>
</listitem></varlistentry><varlistentry><term><literal>SaveStylesheet(name)</literal></term><listitem>
<para>
Save given TrEd's stylesheet (to ~/.tred.d/stylesheets/name).
</para>
</listitem></varlistentry><varlistentry><term><literal>SaveStylesheets()</literal></term><listitem>
<para>
Save all TrEd's stylesheets (to ~/.tred.d/stylesheets/).
</para>
</listitem></varlistentry><varlistentry><term><literal>ReloadStylesheet(name,dir?)</literal></term><listitem>
<para>
Reload a given stylesheet. If no dir is specified, the
default path "~/.tred.d/stylesheets/" is used.
</para>
</listitem></varlistentry><varlistentry><term><literal>ReloadStylesheets(dir?)</literal></term><listitem>
<para>
Reload stylesheets from a given file or directory. If no filename is
specified, the default path "~/.tred.d/stylesheets/" is used.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetStylesheetPatterns(stylesheet)</literal></term><listitem>
<para>
For a given stylesheet, return it's patterns. In a scalar context,
returns a string consisting of all patterns, including the hint.  In
the array context returns three scalars: the first one containing the
text of the hint pattern, the second the context pattern (regexp) and
the other a reference to a list containing the stylesheet
patterns. Returns empty list in case of failure.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetPatternsByPrefix(prefix,stylesheet?)</literal></term><listitem>
<para>
Return all patterns of a given stylesheet starting with a given prefix.
If no stylesheet name is given, a current stylesheet is used.
</para>
</listitem></varlistentry><varlistentry><term><literal>StylesheetExists(stylesheet)</literal></term><listitem>
<para>
Returns true if stylesheet with a given name exists.
</para>
</listitem></varlistentry><varlistentry><term><literal>Stylesheets()</literal></term><listitem>
<para>
Returns a list of TrEd's stylesheet names.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetCurrentStylesheet()</literal></term><listitem>
<para>
Returns name of the stylesheet currently selected for the active
window.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetCurrentStylesheet(stylesheet_name)</literal></term><listitem>
<para>
Set stylesheet for the active window.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetSpecialPattern(prefix)</literal> - OBSOLETE!!</term><listitem>
<para>
This macro is obsoleted by GetPatternsByPrefix.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetDisplayAttrs(pattern,...)</literal> - OBSOLETE!!</term><listitem>
<para>
Setup given patterns as a stylesheet of
the currently displayed <link linkend="">Treex::PML::Document</link>. This does not include
a hint pattern.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetBalloonPattern(string,...)</literal> - OBSOLETE!!</term><listitem>
<para>
Use given strings as a <literal>hint:</literal> pattern for
the currently displayed <link linkend="">Treex::PML::Document</link>.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetDisplayAttrs()</literal> - OBSOLETE!!</term><listitem>
<para>
Get patterns of the currently displayed <link linkend="">Treex::PML::Document</link>'s stylesheet, except
for a <literal>hint:</literal> pattern.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetBalloonPattern()</literal> - OBSOLETE!!</term><listitem>
<para>
Get a <literal>hint:</literal> pattern of the currently displayed
<link linkend="">Treex::PML::Document</link>'s stylesheet.
</para>
</listitem></varlistentry><varlistentry><term><literal>CustomColor(name,new-value?)</literal></term><listitem>
<para>
Get or set user defined custom color.
</para>
</listitem></varlistentry><varlistentry><term><literal>UserConf(name,new-value?)</literal></term><listitem>
<para>
Get or set value of a user defined configuration option.
</para>
</listitem></varlistentry><varlistentry><term><literal>AddStyle(styles,object,key =&gt; value,...)</literal></term><listitem>
<para>
Auxiliary funcion: add styles for an object to a given
style-hash (can be used e.g. from node_style_hook).
</para>
</listitem></varlistentry><varlistentry><term><literal>GetStyles(styles,object,$feature?)</literal></term><listitem>
<para>
Auxiliary funcion: if feature is given, retrieves and returns a
feature of a particular object from the given style-hash.  If feature
is not given or undef, returns a (flat) list of feature =&gt; value pairs
consisting of the style features of a given type of object obtained
from the style-hash.
</para>
<para>
This function can be used e.g. from node_style_hook.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Context API</title>
<variablelist><varlistentry><term><literal>CurrentContext()</literal></term><listitem>
<para>
Return the name of the current macro context.
</para>
</listitem></varlistentry><varlistentry><term><literal>SwitchContext(context)</literal></term><listitem>
<para>
Switch to given macro context.
</para>
</listitem></varlistentry><varlistentry><term><literal>CurrentContextForWindow(win)</literal></term><listitem>
<para>
Get a macro context currently selected in a given window.
</para>
</listitem></varlistentry><varlistentry><term><literal>SwitchContextForWindow(win,context)</literal></term><listitem>
<para>
Switch given window to given macro context.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Treex::PML::FSFormat API</title>
<para>
Here are described for working with <link linkend="">Treex::PML::FSFormat</link> objects. Beside these
macros, <link linkend="">Treex::PML::FSFormat</link> object methods can be used.
</para>
<variablelist><varlistentry><term><literal>FS()</literal></term><listitem>
<para>
Return <link linkend="">Treex::PML::FSFormat</link> object associated with the current <link linkend="">Treex::PML::Document</link>.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetOrd(node)</literal></term><listitem>
<para>
Return value of the special numbering FS attribute. This macro
actually returns the same value as
<literal>$node-&gt;{FS()-&gt;order()}</literal>
</para>
</listitem></varlistentry><varlistentry><term><literal>Attributes(node?,normal_fields?)</literal></term><listitem>
<para>
If no node is given or the node is not associated with a type, return
a list of names of all attributes declared in the FS-file header or
Schema.
</para>
<para>
If node is associated with a type and the 2nd attribute is undef or
false, return list of paths to all its (possibly nested) atomic-value
attributes.  This is equivalent to
</para>
<literallayout>
$node-&gt;type-&gt;schema-&gt;attributes($node-&gt;type)
</literallayout>
<para>
If node is associated with a type and the 2nd attribute is true,
return only first-level attribute names. This is equivalent to
</para>
<literallayout>
$node-&gt;type-&gt;get_normal_fields()
</literallayout>
</listitem></varlistentry><varlistentry><term><literal>SubstituteFSHeader(declarations)</literal></term><listitem>
<para>
Substitute a new FS header for current document. A list of valid FS
declarations must be passed to this function.
</para>
</listitem></varlistentry><varlistentry><term><literal>AppendFSHeader(declarations)</literal></term><listitem>
<para>
Merge given FS header declarations with the present header
of the current document.
</para>
</listitem></varlistentry><varlistentry><term><literal>UndeclareAttributes(attribute,...)</literal></term><listitem>
<para>
Remove declarations of given attributes from the FS header
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Treex::PML::Document I/O API</title>
<para>
See also <link linkend="">Treex::PML::Document</link> object methods defined in the <link linkend="">Treex::PML</link> module.
</para>
<variablelist><varlistentry><term><literal>ChangingFile(0|1)</literal></term><listitem>
<para>
If no argument given the default is 1. If <literal>$FileChanged</literal> is already
set to 1, does nothing. If <literal>$FileChanged</literal> has not yet been
assigned or is zero, sets it to the given value. Returns the resulting
value. <literal>ChangingFile(1)</literal> also resets <literal>$forceFileSaved</literal> to 0.
</para>
</listitem></varlistentry><varlistentry><term><literal>CurrentFile()</literal></term><listitem>
<para>
Return a <link linkend="">Treex::PML::Document</link> object representing the currently processed file.
</para>
</listitem></varlistentry><varlistentry><term><literal>Open(filename,flags)</literal></term><listitem>
<para>
Open a given <link linkend="">Treex::PML::Document</link> in TrEd. Flags is an optional HASH reference
that may contain various flags internally used by TrEd.
</para>
</listitem></varlistentry><varlistentry><term><literal>OpenSecondaryFiles($fsfile)</literal></term><listitem>
<para>
Open secondary files for a given <link linkend="">Treex::PML::Document</link> in TrEd.
</para>
</listitem></varlistentry><varlistentry><term><literal>ReloadCurrentFile()</literal></term><listitem>
<para>
Close and reload current fsfile.
</para>
</listitem></varlistentry><varlistentry><term><literal>Resume($fsfile)</literal></term><listitem>
<para>
Resume a previously open fsfile a given window in TrEd.
</para>
</listitem></varlistentry><varlistentry><term><literal>CloseFile(file?)</literal></term><listitem>
<para>
Close a given <link linkend="">Treex::PML::Document</link>.
</para>
</listitem></varlistentry><varlistentry><term><literal>CloseFileInWindow(window?)</literal></term><listitem>
<para>
This macro is for TrEd only. It closes the <link linkend="">Treex::PML::Document</link> open in the given or
current window.  If the file is open in other windows, it is kept
among postponed files. If the file is modified, the user is asked to
save it.
</para>
</listitem></varlistentry><varlistentry><term><literal>Save()</literal></term><listitem>
<para>
Save the current <link linkend="">Treex::PML::Document</link>.
</para>
</listitem></varlistentry><varlistentry><term><literal>SaveAs({ option=</literal>value,... })&gt;</term><listitem>
<para>
NOTE: This macro is currently only available in TrEd.
</para>
<para>
Save the current <link linkend="">Treex::PML::Document</link> under a new filename.
Returns 1 if the file was saved successfully.
</para>
<para>
Options:
</para>
<variablelist><varlistentry><term>filename</term><listitem>
<para>
The new-filename. If undefined, the usual "Save As..." dialog prompts
the user to select the output filename.
</para>
</listitem></varlistentry><varlistentry><term>fsfile</term><listitem>
<para>
operate on a given fsfile rather than the current one
</para>
</listitem></varlistentry><varlistentry><term>backend</term><listitem>
<para>
save using a given I/O backend
</para>
</listitem></varlistentry><varlistentry><term>update_refs</term><listitem>
<para>
update references from other files to this file.  Values can be:
'ask', 'all', an array of <link linkend="">Treex::PML::Document</link> objects; all
other values mean no update. Default is 'ask'.
</para>
</listitem></varlistentry><varlistentry><term>update_filelist</term><listitem>
<para>
update references from the current filelist to this file.  Values can
be: 'ask', 'current' (update only the current position);
any other value means no update. Default is 'ask'.
</para>
</listitem></varlistentry></variablelist></listitem></varlistentry><varlistentry><term><literal>GetOpenFiles()</literal></term><listitem>
<para>
Return a list of <link linkend="">Treex::PML::Document</link> objects currently open in TrEd (including
postponed files).
</para>
</listitem></varlistentry><varlistentry><term><literal>Backends()</literal></term><listitem>
<para>
Return a list of currently registered I/O backends.
</para>
</listitem></varlistentry><varlistentry><term><literal>AddBackend($classname,$before_backend)</literal></term><listitem>
<para>
Register a new I/O backend. Note that the caller is responsible for
loading the required module (e.g. by calling <link linkend="">Treex::PML::ImportBackends</link>());
If $before_backend is defined, the new backend is added before a given
backend. Otherwise the backend is added as a last backend.
</para>
</listitem></varlistentry><varlistentry><term><literal>RemoveBackend($classname)</literal></term><listitem>
<para>
Remove (unregister) a given backend.
</para>
</listitem></varlistentry><varlistentry><term><literal>AddNewFileList($filelist)</literal></term><listitem>
<para>
This macro is only available in TrEd.
Creates a new TrED filelist from a given <literal>Filelist</literal> object.
</para>
</listitem></varlistentry><varlistentry><term><literal>RemoveFileList($filelist_object_or_name)</literal></term><listitem>
<para>
This macro is only available in TrEd.  It disposes of a given filelist
(identified either by a <literal>Filelist</literal> object or by name) by removing it
from TrEd's internal list of open filelists.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetCurrentFileList($win?)</literal></term><listitem>
<para>
This macro is only available in TrEd. It returns the current file-list
(a Filelist object) of this or a given window.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetFileList($name)</literal></term><listitem>
<para>
This macro is only available in TrEd.
Finds a filelist of a given name in TrEd's internal list open filelists
and returns the corresponding <literal>Filelist</literal> object. Returns undef if not found.
</para>
</listitem></varlistentry><varlistentry><term><literal>AbsolutizeFileName($relative_path,$base_path)</literal></term><listitem>
<para>
Converts a relative path to an absolute path.  The returned value is
an absolute path or URI, computed relative to a given base_path (file
name or URI). The function is able to strip TrEd position suffixes
(##N, ##N.M, and #ID) from the relative path and reattach them to the
returned absolute path.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetCurrentFileList($name)</literal></term><listitem>
<para>
This macro is only available in TrEd. Selects the filelist of a given
name for the current window.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetCurrentFileListInWindow($name, $win)</literal></term><listitem>
<para>
This macro is only available in TrEd. Selects the filelist of a given
name for the given window.
</para>
</listitem></varlistentry><varlistentry><term><literal>TrEdFileLists()</literal></term><listitem>
<para>
This macro is only available in TrEd. It returns all registered
filelists.
</para>
</listitem></varlistentry><varlistentry><term><literal>GetFileSaveStatus()</literal></term><listitem>
<para>
Return 1 if the document was modified since last save or reload, 0
otherwise.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetFileSaveStatus()</literal></term><listitem>
<para>
Use SetFileSaveStatus(1) to declare that some modification was made to
the file. Use SetFileSaveStatus(0) after the file was saved from a
macro (and TrEd/bTrEd would not notice that).
</para>
</listitem></varlistentry><varlistentry><term><literal>DefaultInputEncoding()</literal></term><listitem>
<para>
Return's TrEd's/bTrEd's default IO encoding.
</para>
</listitem></varlistentry><varlistentry><term><literal>SetDefaultInputEncoding(encoding)</literal></term><listitem>
<para>
Set TrEd's/bTrEd's default IO encoding.
</para>
</listitem></varlistentry><varlistentry><term><literal>FileName()</literal></term><listitem>
<para>
Return current file's name.
</para>
</listitem></varlistentry><varlistentry><term><literal>FileMetaData(key,value?)</literal></term><listitem>
<para>
Get or set meta data associated with the current <link linkend="">Treex::PML::Document</link>.  Key is the
meta data key. If value is omitted, current value associated with the
key is returned. Otherwise, the given value is associated with the
key, overwritting any previous value.
</para>
</listitem></varlistentry><varlistentry><term><literal>FileUserData(key,value?)</literal></term><listitem>
<para>
Get or set user data associated with the current <link linkend="">Treex::PML::Document</link>.  Key is the
user data key. If value is omitted, current value associated with the
key is returned. Otherwise, the given value is associated with the
key, overwritting any previous value.
</para>
</listitem></varlistentry><varlistentry><term><literal>FileAppData(key,value?)</literal></term><listitem>
<para>
Get or set application specific data associated with the current
<link linkend="">Treex::PML::Document</link>.  Key is the appData key. If value is omitted, current value
associated with the key is returned. Otherwise, the given value is
associated with the key, overwritting any previous value.
</para>
</listitem></varlistentry><varlistentry><term><literal>GotoFileNo(n)</literal></term><listitem>
<para>
Goto n'th file in the current filelist.
The number of the first file in filelist is 0.
</para>
</listitem></varlistentry><varlistentry><term><literal>LastFileNo($win?)</literal></term><listitem>
<para>
Return the index of the last file in the current filelist.
</para>
</listitem></varlistentry><varlistentry><term><literal>CurrentFileNo($win?)</literal></term><listitem>
<para>
Return the index of the current file in the current filelist.
</para>
</listitem></varlistentry><varlistentry><term><literal>SaveAndNextFile()</literal></term><listitem>
<para>
Save the current file and open the next file in the current file-list.
</para>
</listitem></varlistentry><varlistentry><term><literal>NextFile()</literal></term><listitem>
<para>
Goto next file in the file-list.
</para>
</listitem></varlistentry><varlistentry><term><literal>SaveAndPrevFile()</literal></term><listitem>
<para>
Save the current file and open the previous file in the current
file-list.
</para>
</listitem></varlistentry><varlistentry><term><literal>PrevFile()</literal></term><listitem>
<para>
Goto previous file in the file-list.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>General I/O macros</title>
<variablelist><varlistentry><term><literal>ResourcePaths()</literal></term><listitem>
<literallayout>
Return the current list of directories used to search for
resources.
</literallayout>
</listitem></varlistentry><varlistentry><term><literal>SetResourcePaths(dirs)</literal></term><listitem>
<para>
Set given list of directories as a current resource path (discarding
the existing values of ResourcePath).
</para>
</listitem></varlistentry><varlistentry><term><literal>AddResourcePath(dirs)</literal></term><listitem>
<para>
Add given directories to the end of the current resource path (to be
searched last).
</para>
</listitem></varlistentry><varlistentry><term><literal>AddToResourcePathAsFirst(dirs)</literal></term><listitem>
<para>
Add given directories to the beginning of the current resource path
(to be searched first).
</para>
</listitem></varlistentry><varlistentry><term><literal>RemoveResourcePath(dir-paths)</literal></term><listitem>
<para>
Remove given directories from the current resource path (directory
paths must exactly match those listed in the resource path).
</para>
</listitem></varlistentry><varlistentry><term><literal>FindDirInResources(dirname)</literal></term><listitem>
<para>
If a given dirname is a relative path of a directory found in TrEd's
resource directory, return an absolute path for the
resource. Otherwise return dirname.
</para>
</listitem></varlistentry><varlistentry><term><literal>FindInResources(filename)</literal></term><listitem>
<para>
If a given filename is a relative path of a file found in TrEd's
resource directory, return an absolute path for the
resource. Otherwise return filename.
</para>
</listitem></varlistentry><varlistentry><term><literal>ResolvePath(ref-filename,filename,use_resources?)</literal></term><listitem>
<para>
If a given filename is a relative path, try to find the file in the
same directory as ref-filename. In case of success, return a path
based on the directory part of ref-filename and filename.  If the file
cannot be located in this way and use_resources is true, return the
value of <literal>FindInResources(filename)</literal>.
</para>
</listitem></varlistentry><varlistentry><term><literal>SlurpURI($filename_or_uri,$chomp?)</literal></term><listitem>
<para>
Given a file name or URI of a resource, the macro returns the content
of the resource. In array context the return value is a list of lines,
in scalar context the value is a scalar. If the chomp flag is true,
chomp() is applied to the returned array or scalar.
</para>
</listitem></varlistentry><varlistentry><term><literal>writeln(string?,...)</literal></term><listitem>
<para>
Print the arguments to standard output appending a new-line if missing.
</para>
</listitem></varlistentry><varlistentry><term><literal>stdout(string?,...)</literal></term><listitem>
<para>
If called without arguments return current standard output filehandle.
Otherwise call print the arguments to standard output.
</para>
</listitem></varlistentry><varlistentry><term><literal>stderr(string?,...)</literal></term><listitem>
<para>
If called without arguments return current standard error output
filehandle.  Otherwise call print the arguments to standard output.
</para>
</listitem></varlistentry><varlistentry><term><literal>tmpFileName()</literal></term><listitem>
<para>
Returns a temporary filename..
</para>
</listitem></varlistentry><varlistentry><term><literal>DirPart($path)</literal></term><listitem>
<para>
Returns directory part of a given path (including volume).
</para>
</listitem></varlistentry><varlistentry><term><literal>FilePart($path)</literal></term><listitem>
<para>
Returns file-name part of a given path.
</para>
</listitem></varlistentry><varlistentry><term><literal>CallerPath()</literal></term><listitem>
<para>
Return path of the perl module or macro-file that invoked this macro.
</para>
</listitem></varlistentry><varlistentry><term><literal>CallerDir($rel_path?)</literal></term><listitem>
<para>
If called without an argument, returns the directory of the perl
module or macro-file that invoked this macro.
</para>
<para>
If a relative path is given as an argument, a respective absolute path
is computed based on the caller's directory and returned.
</para>
</listitem></varlistentry><varlistentry><term><literal>FindMacroDir($rel_dir)</literal></term><listitem>
<para>
Searches for a given relative path in directories with user-defined
macros and returns an absolute path to the first directory that matches.
</para>
</listitem></varlistentry></variablelist>
</section>
<section id="sect-0">
<title>Printing trees</title>
<variablelist><varlistentry><term><literal>PrintDialog(-option =&gt; value,...)</literal></term><listitem>
<para>
Display TrEd's standard print dialog. Possible options providing
substitutes for the default values in the print dialog are:
</para>
<variablelist><varlistentry><term><literal>-command</literal></term><listitem>
<para>
System command to send the output to (e.g. <literal>lpr</literal> to print on the
default printer on UNIX platform).
</para>
</listitem></varlistentry><varlistentry><term><literal>-toFile</literal></term><listitem>
<para>
If set to 1, the output is saved to a file specified in <literal>-filename</literal>.
</para>
</listitem></varlistentry><varlistentry><term><literal>-filename</literal></term><listitem>
<para>
Output filename.
</para>
</listitem></varlistentry><varlistentry><term><literal>-fileExtension</literal></term><listitem>
<para>
Default output file extension.
</para>
</listitem></varlistentry><varlistentry><term><literal>-format</literal></term><listitem>
<para>
One of PS, PDF, EPS, ImageMagick. Default is PS.
</para>
</listitem></varlistentry><varlistentry><term><literal>-imageMagickResolution</literal></term><listitem>
<para>
This value is passed to the command <literal>convert</literal> of the ImageMagick
toolkit as <literal>-density</literal>. It specifies the horizontal and vertical
resolution in pixels of the image.
</para>
</listitem></varlistentry><varlistentry><term><literal>-noRotate</literal></term><listitem>
<para>
Disable automatic landscape rotation of trees which are wider than
taller.
</para>
</listitem></varlistentry><varlistentry><term><literal>-sentenceInfo</literal></term><listitem>
<para>
If set to 1, this command prints also the text associated with the
tree. Instead of 0 or 1, an CODE reference (subroutine) may be passed
in this parameter. This CODE is then evaluated for every tree to
produce the desired text. The CODE obtains two arguments: the current
<link linkend="">Treex::PML::Document</link> object and an integer position of the tree (starting from 0).
</para>
</listitem></varlistentry><varlistentry><term><literal>-fileInfo</literal></term><listitem>
<para>
If true, print filename and tree number under each tree.
</para>
</listitem></varlistentry><varlistentry><term><literal>-colors</literal></term><listitem>
<para>
Set to 1 for colour output.
</para>
</listitem></varlistentry></variablelist></listitem></varlistentry><varlistentry><term><literal>Print(-option =&gt; value,...)</literal></term><listitem>
<para>
Print trees given from current file according to given printing options:
</para>
<variablelist><varlistentry><term><literal>-range</literal></term><listitem>
<para>
Lists trees to be printed (e.g. <literal>5,-3,9-12,15-</literal> prints trees
5,1,2,3,9,10,11,12,15,16,...)
</para>
</listitem></varlistentry><varlistentry><term><literal>-to</literal></term><listitem>
<para>
Possible values: file (print to file specified by <literal>-filename</literal>),
(send output to ImageMagick convert, see <literal>-convert</literal>),
pipe (send output to the standard input of command <literal>-command</literal>),
string (return output as a string), object (return output as an object -
the exact type of the value depends on the output format).
</para>
</listitem></varlistentry><varlistentry><term><literal>-command</literal></term><listitem>
<para>
System command to send the output to (usually default to <literal>lpr</literal> on UNIX platform).
</para>
</listitem></varlistentry><varlistentry><term><literal>-convert</literal></term><listitem>
<para>
Path to ImageMagick 'convert' command.
</para>
</listitem></varlistentry><varlistentry><term><literal>-filename</literal></term><listitem>
<para>
Output filename (only when printing to file).
</para>
</listitem></varlistentry><varlistentry><term><literal>-format</literal></term><listitem>
<para>
One of PS, PDF, EPS, ImageMagick. Default is PS.
</para>
</listitem></varlistentry><varlistentry><term><literal>-noRotate</literal></term><listitem>
<para>
Disable automatic landscape rotation of trees which are wider than
taller.
</para>
</listitem></varlistentry><varlistentry><term><literal>-sentenceInfo</literal></term><listitem>
<para>
If set to 1, this command prints also the text associated with the
tree. Instead of 0 or 1, a CODE reference (subroutine) may be passed
in this parameter. This CODE is then evaluated for every tree to
produce the desired text. The CODE obtains two arguments: the current
<link linkend="">Treex::PML::Document</link> object and an integer position of the tree (starting from 0).
</para>
</listitem></varlistentry><varlistentry><term><literal>-fileInfo</literal></term><listitem>
<para>
If true, print filename and tree number under each tree.
</para>
</listitem></varlistentry><varlistentry><term><literal>-imageMagickResolution</literal></term><listitem>
<para>
This value is passed to the command <literal>convert</literal> of the ImageMagick
toolkit as <literal>-density</literal>. It specifies the horizontal and vertical
resolution in pixels of the image.
</para>
</listitem></varlistentry><varlistentry><term><literal>-colors</literal></term><listitem>
<para>
Set to 1 for colour output.
</para>
</listitem></varlistentry><varlistentry><term><literal>-hidden</literal></term><listitem>
<para>
Set to 1 to print hidden nodes.
</para>
</listitem></varlistentry><varlistentry><term><literal>-psFontFile</literal></term><listitem>
<para>
Specifies the PostScript font file to be used instead of the default
one.
</para>
</listitem></varlistentry><varlistentry><term><literal>--psFontAFMFile</literal></term><listitem>
<para>
Specifies the PostScript ASCII metric font file to be used instead of
the default one.
</para>
</listitem></varlistentry><varlistentry><term><literal>-ttFont</literal></term><listitem>
<para>
Specifies the TrueType font file to be used when printing via PDF.
</para>
</listitem></varlistentry><varlistentry><term><literal>-fontSize</literal></term><listitem>
<para>
Font size.
</para>
</listitem></varlistentry><varlistentry><term><literal>-fmtWidth</literal></term><listitem>
<para>
Page width.
</para>
</listitem></varlistentry><varlistentry><term><literal>-fmtHeight</literal></term><listitem>
<para>
Page height.
</para>
</listitem></varlistentry><varlistentry><term><literal>-hMargin</literal></term><listitem>
<para>
The size of the left and right horizontal margins.
</para>
</listitem></varlistentry><varlistentry><term><literal>-vMargin</literal></term><listitem>
<para>
The size of the top and bottom margins.
</para>
</listitem></varlistentry><varlistentry><term><literal>-maximize</literal></term><listitem>
<para>
Expand small trees to fit the whole page size. (Shrinking is done
automatically).
</para>
</listitem></varlistentry><varlistentry><term><literal>-psMedia</literal></term><listitem>
<para>
Specifies target given media size (used for PostScript and PDF).
Possible values:
</para>
<para>
User (dimensions specified in -fmtHeight and -fmtWidth), BBox
(bounding box of the tree with only -hMargin and -vMargin added),
Letter, LetterSmall, Legal, Statement, Tabloid, Ledger, Folio, Quarto,
Executive, A0, A1, A2, A3, A4, A4Small, A5, A6, A7, A8, A9, A10, B0,
B1, B2, B3, B4, B5, B6, B7, B8, B9, B10, ISOB0, ISOB1, ISOB2, ISOB3,
ISOB4, ISOB5, ISOB6, ISOB7, ISOB8, ISOB9, ISOB10, C0, C1, C2, C3, C4,
C5, C6, C7, 7x9, 9x11, 9x12, 10x13, 10x14
</para>
</listitem></varlistentry><varlistentry><term><literal>-stylesheet</literal></term><listitem>
<para>
Name of the stylesheet to use (defaults to the current stylesheet).
</para>
</listitem></varlistentry><varlistentry><term><literal>-toplevel</literal></term><listitem>
<para>
Toplevel window (e.g. ToplevelFrame()) if you wish for graphical progress
indicator.
</para>
</listitem></varlistentry></variablelist></listitem></varlistentry></variablelist>
</section>
<section>
<title>Other macros</title>
<variablelist><varlistentry><term><literal>quit()</literal></term><listitem>
<para>
This command acts just like a <literal>die</literal> but without producing any error
message at all. It can be used to immediatelly stop execution of the
macro code and returning control to TrEd or btred (in which case btred
starts processing the next file) but does not propagate through
<literal>eval{}</literal>, so it can also be trapped.
</para>
<para>
Note that hooks that my follow execution of the macro code (such as
file_close_hook or exit_hook in btred) are executed as if the macro
ended normally.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>Implementation of TredMacro::import</title>
<variablelist><varlistentry><term><literal>import(names?)</literal></term><listitem>
<para>
If specified without parameter, exports every symbol to the caller
package (except for symbols already (re)defined in the caller
package). If parameters are given, exports only names specified by
the parameters and the following few variables that every package
derived from TredMacro (e.g. a context)  <emphasis>must</emphasis> share: <literal>$libDir</literal>,
<literal>$grp</literal>, <literal>$root</literal>, <literal>$this</literal> <literal>$_NoSuchTree</literal> <literal>$Redraw</literal>,
<literal>$forceFileSaved</literal>, <literal>$FileChanged</literal>, <literal>$FileNotSaved</literal>,
<literal>$NodeClipboard</literal>.
</para>
</listitem></varlistentry></variablelist>
</section>
<section>
<title>XPath extension (slow)</title>
<variablelist><varlistentry><term><literal>SetupXPath(function-mapping...)</literal></term><listitem>
<literallayout><literal>SetupXPath(
            id         =&gt; \&amp;find_node_by_id,
            pos        =&gt; \&amp;node_position,
            attributes =&gt; \&amp;node_attributes_hashref,
            name       =&gt; \&amp;node_name,
            value      =&gt; \&amp;node_value,
            children   =&gt; \&amp;node_children,
            parent     =&gt; \&amp;node_parent,
            lbrother   =&gt; \&amp;node_left_sibling,
            rbrother   =&gt; \&amp;node_right_sibling,
           )</literal></literallayout>
<para>
This macro requires <literal>XML::XPath</literal> module to be installed.  It adjusts
<link linkend="">Treex::PML::Node</link> API to match XPath model based on a given function mapping.  By
default, 'id' is defined to return nothing, 'pos' returns nodes
sentence-ordering position (or depth-first-ordering position if sentord
attribute is not defined), 'attributes' returs a hashref of node's
attributes (actually a <link linkend="">Treex::PML::Node</link> itself), 'name' returns "node", 'value'
returns node's value of the special FS value attribute, and
'children', 'parent', 'lbrother', and 'rbrother' all default to the
respective <link linkend="">Treex::PML::Node</link> methods.
</para>
<para>
Usage example:
</para>
<literallayout><literal>SetupXPath(id    =&gt; sub { $hashed_ids{ $_[0] } },
           name  =&gt; sub { $_[0]-&gt;{functor} }
           value =&gt; sub { $_[0]-&gt;{t_lemma} });</literal></literallayout>
<literallayout><literal>foreach ($node-&gt;findnodes(q{//ACT/PAT[starts-with(@tag,"N") or .="ano"]})) {
    # process matching nodes
}</literal></literallayout>
</listitem></varlistentry></variablelist>
</section>
</section>
</article>
